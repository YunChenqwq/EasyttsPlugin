# EasyttsPlugin（MaiBot 插件目录）

把整个目录 `EasyttsPlugin/` 复制到 MaiBot 的 plugins 目录即可使用。

必读：配置文件在 `EasyttsPlugin/config.toml`。

命令：
- `/eztts <文本> [-v 角色:预设] [-e 情绪]`

开源协议：
- 本目录按 AGPL-3.0 分发（参考并改造自 xuqian13/tts_voice_plugin）。
- 详见本目录 `NOTICE.md` 与 `LICENSE`。
